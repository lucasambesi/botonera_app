name: botonera_app
description: Botonera App.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^0.8.0
  color_parser: ^0.0.2
  cupertino_icons: ^1.0.0
  esys_flutter_share: ^1.0.1
  flutter_colorpicker: ^0.3.5
  path_provider: ^0.5.0
  provider: ^4.3.3
  sqflite: null
  admob_flutter: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/audios/
